"use client";
import Link from"next/link";import{usePathname}from"next/navigation";
const pages=[["01","Automatización","/servicios/smart-homes"],["02","Servicios","/servicios"],["03","Proyectos","/proyectos"],["04","Proceso","/proceso"],["05","Nosotros","/nosotros"],["06","Contacto","/contacto"]]as const;
const currentHref=(p:string)=>p.startsWith("/servicios/smart-homes")?"/servicios/smart-homes":"/"+(p.split("/").filter(Boolean)[0]||"");
export function PageRail(){const path=usePathname(),current=currentHref(path),index=pages.findIndex(([, ,href])=>href===current);if(path==="/"||index<0)return null;const next=pages[(index+1)%pages.length];return <aside className="pointer-events-none fixed right-5 top-1/2 z-30 hidden -translate-y-1/2 2xl:block"><nav aria-label="Secciones principales" className="pointer-events-auto flex flex-col items-end gap-3">{pages.map(([n,label,href])=>{const active=href===current;return <Link key={href} href={href} aria-current={active?"page":undefined} className="group flex items-center gap-3"><span className={`overflow-hidden whitespace-nowrap font-mono text-[8px] uppercase tracking-[.18em] transition-all duration-300 ${active?"w-auto translate-x-0 text-brand-gold":"w-0 translate-x-2 text-ca-text/45 group-hover:w-auto group-hover:translate-x-0"}`}>{label}</span><span className={`grid h-8 w-8 place-items-center border font-mono text-[8px] transition ${active?"border-brand-gold bg-brand-gold text-[#07111d]":"border-ca-border text-ca-text/45 group-hover:border-brand-gold/60 group-hover:text-brand-gold"}`}>{n}</span></Link>})}</nav><Link href={next[2]} className="pointer-events-auto mt-8 flex items-center gap-3 font-mono text-[8px] uppercase tracking-[.18em] text-ca-text/45 transition hover:text-brand-gold"><span>Siguiente</span><span className="grid h-9 w-9 place-items-center border border-ca-border">↓</span></Link></aside>}
export default PageRail;