"use client";
import {useEffect} from "react";import {usePathname} from "next/navigation";import {gsap} from "gsap";import {ScrollTrigger} from "gsap/ScrollTrigger";import {Header} from "./Header";import {WhatsAppButton} from "./WhatsAppButton";import {Footer} from "./Footer";import {InjectIconStyles} from "./icons/AnimatedIcons";import {SmoothScroll} from "./SmoothScroll";import {RouteScape} from "./RouteScape";import {PageRail} from "./PageRail";import {ZenitMotionSystem} from "./ZenitMotionSystem";import {BackgroundParticles} from "./BackgroundParticles";import {CustomCursor} from "./CustomCursor";import {ScrollChargeNav} from "./ScrollChargeNav";
gsap.registerPlugin(ScrollTrigger);
export function ClientWrapper({children}:{children:React.ReactNode}){const pathname=usePathname();useEffect(()=>{window.scrollTo(0,0);const frame=requestAnimationFrame(()=>ScrollTrigger.refresh());const timer=window.setTimeout(()=>ScrollTrigger.refresh(),500);return()=>{cancelAnimationFrame(frame);clearTimeout(timer)}},[pathname]);useEffect(()=>{const bar=document.getElementById("scroll-progress");if(!bar)return;const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;bar.style.transform=`scaleX(${max>0?scrollY/max:0})`};update();addEventListener("scroll",update,{passive:true});addEventListener("resize",update);return()=>{removeEventListener("scroll",update);removeEventListener("resize",update)}},[]);const shell=pathname!=="/about/conexiones";return <div className="relative flex min-h-screen flex-col overflow-x-clip bg-ca-bg-deep font-sans text-brand-light antialiased selection:bg-brand-gold selection:text-brand-dark"><InjectIconStyles/><SmoothScroll/><ZenitMotionSystem/><div className="grain-overlay"/><div className="zenit-vignette pointer-events-none fixed inset-0 z-[2]"/><div className="bg-blob-1 pointer-events-none fixed left-[5%] top-[12%] z-0 h-[34rem] w-[34rem] rounded-full bg-brand-gold/5 blur-[130px]"/><div className="bg-blob-2 pointer-events-none fixed right-[3%] top-[42%] z-0 h-[44rem] w-[44rem] rounded-full bg-ca-bg-primary/35 blur-[150px]"/><BackgroundParticles/><CustomCursor/><RouteScape/><div id="scroll-progress" aria-hidden="true" className="fixed left-0 top-0 z-[9100] h-[2px] w-full origin-left scale-x-0 bg-brand-gold"/>{shell&&<Header/>}{pathname==="/"?<ScrollChargeNav/>:shell?<PageRail/>:null}<main id="main-content" tabIndex={-1} className="relative z-10 w-full flex-grow bg-transparent outline-none">{children}</main><WhatsAppButton variant="floating" label="Enviar foto y medidas"/>{shell&&<Footer/>}</div>}
export default ClientWrapper;